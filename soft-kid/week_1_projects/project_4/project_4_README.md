## PROJECT 4 -------SYSTEM STATUS DASHBOARD---------

# Below is how the system functions:
 - Reads any CSV file with sensor data
 - Automatically produce a text-base dashboard with;
   - total records
   - anomaly counts
   - mean/max/min per column
- it generate's a simple ASCII bar chart of value distribution
- it accept the filename using the sys.avg command line

Run code for siimulation
